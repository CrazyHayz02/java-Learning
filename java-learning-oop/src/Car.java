public class Car {
    String make;
    String model;
    int year;

    void displayInfo() {
        System.out.println(
                "The Car is the " + make + " " + model + " " + year + "."
        );

    }
}
